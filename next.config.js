/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['moein.video'],
        output: {
          export: true
        }
      },
}

module.exports = nextConfig
